{
  "name": "symphony-ts",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "format": "pnpm dlx @biomejs/biome@2.2.3 format --write .",
    "format:check": "pnpm dlx @biomejs/biome@2.2.3 format --check .",
    "lint": "pnpm dlx @biomejs/biome@2.2.3 lint .",
    "test": "pnpm -r test && node --test tests/**/*.test.mjs",
    "typecheck": "pnpm -r typecheck",
    "db:types": "supabase gen types typescript --local > packages/shared/src/db-types.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3"
  }
}
